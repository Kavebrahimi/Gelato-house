import { Button } from "../base/button"



const Header: React.FC = () => {
  return (
    <header>
      <Button variant="contained" color="primary">Jelato</Button>
    </header>
  )
}

export default Header
